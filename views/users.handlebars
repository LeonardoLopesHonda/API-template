<section>
    <a href="/" class="return"><i class="fas fa-angle-left"></i>Voltar</a>
    <h1>See here who's registered:</h1>
    {{#each users}}
    <div class="row">
        <div>
            <h1>{{nome}}</h1>
        </div>
        <div class="group-buttons">
            <a href="/delete/{{id}}"><button class="delete">Excluir</button></a>
        </div>
    </div>
    {{/each}}
    <a class="form" href="/registerForm">Form</a>
</section>
<style>
    section {
        width: 90vw;
        height: 83vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin: auto;


        padding: 3rem;
        border: 1px solid black;
        border-radius: 10px;
    }

    .row {
        width: 100%;

        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 4rem;
    }

    .group-buttons {
        display: flex;
        align-items: center;
        justify-content: space-around;
    }

    .delete {
        padding: 1rem;
        color: white;
        border-radius: 10px;
        background-color: red;

        transition: all 500ms;
    }

    .delete:hover {
        cursor: pointer;
        border-radius: 0px;
        background-color: #bf0000;
    }

    .form {
        margin-top: 1rem;

        font-size: x-large;
        font-family: 'Times New Roman', Times, serif;
        text-decoration: none;
        color: black;

        padding: 1.15rem;
        padding-right: 1.5rem;
        padding-left: 1.5rem;
        border: 1px solid #2c282c;
        border-radius: 10px;

        transition: all 500ms,
            color 800ms 150ms;
    }

    .form:hover {
        border: none;
        border-radius: 0px;
        background: #000;
        color: white;
    }

    .return {
        padding: 1rem;
        padding-left: 1.25rem;
        padding-right: 1.25rem;
        border-radius: 10px;
        background-color: #2c282c;

        color: white;
        font-size: larger;
        text-decoration: none;


        display: flex;
        align-items: first baseline;
        justify-content: center;
        gap: .5rem;
        place-self: start;

        transition: all 500ms;
    }

    .return:hover {
        cursor: pointer;
        border-radius: 0px;
        background-color: #635a63;
    }
</style>